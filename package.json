{
  "name": "anton",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end encrypted chat on AXL + ENS.",
  "license": "MIT",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "pnpm --filter @anton/desktop dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "tauri": "pnpm --filter @anton/desktop tauri",
    "cargo:repair-target": "node ./scripts/cargo-clean-workspace-target.mjs",
    "contracts:build": "cd contracts && forge build",
    "contracts:test": "cd contracts && forge test"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
